<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>memo list</title>
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
    <h1>memo list</h1>
    <ul>
      <% items.forEach((item) => { %>
        <li>
          <div class="memo-data">
            <span><%= item.id %></span>
            <span><%= item.memo %></span>
          </div>
        </li>
      <% }); %>
    </ul>
  </body>
</html>