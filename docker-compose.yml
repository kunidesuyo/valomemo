version: '3.7'
services:

  db:
    image: mysql:8.0.31

    environment:
      - MYSQL_ROOT_PASSWORD=password
      - MYSQL_DATABASE=valomemo

    ports:
      - '3306:3306'

    volumes:
      - ./db/conf:/etc/mysql/conf.d/:ro

    networks:
      - backend

    healthcheck:
      test: ["CMD", "mysql", "-uroot", "-ppassword"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
    - 8080:80
    volumes:
      - type: bind
        source: ./app/src
        target: /usr/app/src
    networks:
      - backend

    depends_on:
      db:
        condition: service_healthy

networks:
  backend: